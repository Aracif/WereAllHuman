
using UnityEngine;

public class PlayerActionPopupTextHandler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Destroy(gameObject, 2f);
        //transform.localPosition += new Vector3(0, 0.5f, 0);
    }
}
